{
 
  "scripts": {
    "start": "node server.js --ignore magicmania",
    "develop": "concurrently \"cd server && npm run watch\" \"cd magicmania && npm start\"",
    "install": "cd server && npm i && cd ../magicmania && npm i",
    "seed": "node db/seeds/magicWordsSeed.js && node db/seeds/userSeed.js",
    "build": "cd magicmania && npm run build"
  },
  
  "dependencies": {
    "@apollo/client": "^3.8.4",
    "apollo-server-express": "^3.12.1",
    "bcrypt": "^5.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
